bool isPalindrome(int y) {
        if(y<0)return false;
        long long rev=0;
        long long x=y;
        while(x!=0){
            long long rem=x%10;
            rev=rev*10+rem;
            x=x/10;
        }
        if(y==rev)return true;
        else return false;
    }
